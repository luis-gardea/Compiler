class Main {
    a : Int <- a + 5;

    a : Int;

    a : Int;
    
    b(x : Int) : Int {
        b <- x
    };

    d : Bool;

    c : Int <- if d then e <- 5 else e <- 6 fi;

    g : Int <- if d then e <- e + e else e fi;

    h: Bool <- "" = 7;

    j: A <- new B;

    k: B;
};
